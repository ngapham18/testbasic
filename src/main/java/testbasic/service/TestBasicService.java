/**
 * This is the Spring Boot POC using Spring boot, Spring Secure, and AWS 
 */
package testbasic.service;

import java.util.List;

import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Service;
/**
import paymentdata.model.Transaction;
import paymentdata.model.TransactionResponse;
import paymentdata.vantiv.AuthorizationRequest;
import paymentdata.vantiv.AuthorizationReversalRequest;
import paymentdata.vantiv.TokenizationRequest;

/**
 * @author x3fm
 *
 */
//@Service
/**

public interface TestBasicService {
	
	List<Transaction> getListOfTransaction();
	ResponseEntity<TransactionResponse> authorization(AuthorizationRequest request);
	ResponseEntity<TransactionResponse> tokenization(TokenizationRequest request);
	ResponseEntity<TransactionResponse> authorizationReversal(AuthorizationReversalRequest requeset);

}
*/
